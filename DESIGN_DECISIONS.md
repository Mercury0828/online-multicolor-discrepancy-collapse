# DESIGN_DECISIONS — `online-multicolor-discrepancy-collapse`

> Append-only decision log. Fixed 3-field rows. Newest at bottom.

| date | decision | evidence/trigger |
|---|---|---|
| 2026-06-22 | Phase 0 launched as orchestrator/referee (NOT solo prover); scaffold + kill-scan + `(★)` de-risk, stop at human gate. | `start_up_for_claude_code.md` bootstrap + `guide.md` constitution. |
| 2026-06-22 | Target venue fixed = SODA; contribution fixed = matching multicolor threshold characterization; NO-RETREAT red line in force. | `guide.md` §Header, §0b. |
| 2026-06-22 | Freeze substrate `P1–P4` / barriers `B1–B3` / refuted `N1–N4`; pose `(★)` as the open crux. Do NOT headline `K_1=Θ(M)` (B3). | `guide.md` §Theory; ledger. |
| 2026-06-22 | Phase-0 kill-scan verdict = GREEN (no scoop); de-risk = `(★)` not refuted, mildly cascade-favorable. Proceed to attack `(★)` (pending human GREEN gate). | `lit/SCAN_REPORT.md`, `lit/SCAN_SECOND_OPINION.md` (82% no-scoop), `code/DERISK_REPORT.md`. |
| 2026-06-22 | P1 quantifier (max-over-prefixes ℓ∞) + d-independence verified against A-T 2509.02432 v3 Def 1.4 / Thm 1.1. | Direct full-text check (auditor self-consistency flag resolved). |
| 2026-06-22 | Record cascade-fork LOWER-bound gap: P2 = matching lower for FAST fork ONLY; cascade needs `HS-lower` (Profile-Branching), zero-proven, B2-gated. Never call P2 "the matching lower" without naming the fork. | Health-check ①; ledger 2026-06-22. |
| 2026-06-22 | Adopt FAST-FIRST sequencing as the recommended attack ordering: bank the fast `O(loglog n/k)` upper (`HS-alt`) as a guaranteed SODA floor before/parallel to the cascade gamble. Brief's "what we need back" updated. (Sequencing note for the human gate, NOT a contribution downgrade — NO-RETREAT intact.) | Health-check ②; `docs/HEALTH_CHECK_phase0.md`. |
| 2026-06-22 | Over-claim rails: keep `K_1` a bracket; state "decreasing in k" as RATE-level (not strict per-k; Sim B flat stretches); "P2 + algorithm" alone ≠ SODA (= the ESA downgrade). | Health-check ③(b)(c)(d). |
| 2026-06-22 | **Human GREEN gate PASSED.** Attack loop authorized. Owner overrides the codex-xhigh default → dispatch DIRECTLY to web GPT-5.5-Pro; owner relays manually (write-file → paste → paste-back). Sequencing = fast-first (recommended, owner-unopposed). | Owner gate answer 2026-06-22. |
| 2026-06-22 | **Round 1 banked: P6** (block-compression `ondisc_k ≤ q·ondisc_{kq}`) added to substrate (audit-verified 98% + 0 numeric violations). First verified gain of the attack loop. | `rounds/round1_audit.md` Audit 1; `code/verify_round1.py`. |
| 2026-06-22 | **Literal `(★)` declared REFUTED** (natural normalization); pivot the cascade target to the pathwise `(★')` (max-cylinder control). NOT a kill (no proof-of-death; `(★')`/FAST open). | `rounds/round1_response.md` §2 (CLAIMs A,B); `rounds/round1_audit.md` Audit 2 (93%). |
| 2026-06-22 | **FAST target re-stated to `(CP-uniform)`** (uniform `ondisc_K=O(1)` across a `Θ(loglog n)` band) + the residual `L≲k≲M` regime + the open `ondisc_K`-monotonicity sub-question — correcting GPT's existential `K_1=O(loglog n)` framing. | Audit 1 (FAST reduction flawed-as-written, 88%). |
| 2026-06-22 | **Round 2 banked P7** (offline `√(d/k)` floor) + **P8** (aggregate cover lemma ⇒ max-cylinder unnecessary). 3 blind audits (93/97/93%) + numerics. | `rounds/round2_audit.md`; `code/verify_round2.py`. |
| 2026-06-22 | **Clean forms REFUTED (N5):** `Θ(max{1,L/k})`/`Θ(max{1,L/log k})` are NOT the full answer; corrected characterization = `Θ(max{1, F_online(k), √(d/k)})`. **Monotonicity REFUTED (N6)** (exact `11/9,13/9,1`). | P7 implication; Audit A/C. |
| 2026-06-22 | **HUMAN GATE (c) fired** (contribution shape + k-regime changed). Owner to choose scope: full-d-range richer theorem / restricted-d clean theorem / offline+small-k; re-confirm SODA. Agent does NOT pick scope (NO-RETREAT: not a downgrade — the corrected form is richer, arguably MORE novel). | Round-2 reshaping; `docs/HEALTH_CHECK_gate_c.md`. |
| 2026-06-22 | **GATE (c) RESOLVED: scope = (a)-ambition + (b)-floor.** Target the full d-range richer theorem `Θ(max{1,F_online,√(d/k)})`; guarantee the `d=O(1)` clean `Θ(max{1,F_online(k)})` as the bankable SODA floor. SODA retained (conditional: matching upper = SODA, lower-only = downgrade). Headline rewrite adopted ("A-T d-independence is k=2-special; d-dependent `√(d/k)` floor for k≥3"). | Owner gate-c answer 2026-06-22. |
| 2026-06-22 | **Round 3 attack = BOTH points in parallel** (owner choice "两路并发"): Target A = matching ONLINE upper for the `√(d/k)` middle regime (does online catch DOWN to the offline floor?) + confirm offline `O(√(d/k))` upper; Target B = the `d=O(1)` `F_online` matching (fast HS-alt+P2, or cascade via P8 (AM) adaptive stability + HS-lower). | Owner gate-c answer; `docs/HEALTH_CHECK_gate_c.md`. |
| 2026-06-22 | **Round 3 banked P9** (occupancy-entropy floor, supersedes P7), **P10** (mean-square, L2-only); **N7** (A1/A2 `√(d/k)` matching upper refuted). 2 blind audits (88/95%) + numerics. **Folklore record CORRECTED:** multicolor offline UPPER is open (only 2-color `√d` is folklore). | `rounds/round3_audit.md`; `code/verify_round3.py`. |
| 2026-06-22 | **STRATEGIC INFLECTION recorded (NOT a decision):** 3 rounds → rich lower-bound/tool theory, ZERO matching upper, target reshaped 3×. Strategy health-check: SODA-by-deadline ≈ 10–15%; reachable result (offline occupancy-threshold char.) ≈ ESA/RANDOM-grade; SODA-grade novelty (online localization) least reachable. → **owner touch (gate a/c)**, agent does NOT downgrade venue. Research line stays alive (escalate, don't weaken). | `docs/HEALTH_CHECK_strategy_r3.md`. |
| 2026-06-22 | **GATE (a/c) RESOLVED: owner KEEPS SODA, full force (NO venue downgrade — NO-RETREAT exercised despite ~10–15% odds).** Round 4 = escalate a FRESH-context attacker on the offline entropy-saturation UPPER (#1) ONLY — bank the complete offline occupancy-threshold characterization first; online-localization remains the SODA-grade prize. NO parallel ESA/RANDOM writing pivot. | Owner gate answer 2026-06-22. |
| 2026-06-22 | 🟢 **Round 4 CLOSED the OFFLINE characterization: `offdisc_k = Θ(Ψ(λ, log k))` (P11)** — the project's FIRST matching result (3 blind audits 92/88/88% + numerics). **Corrected P9** (the round-3 intermediate-band scale was wrong). The owner-authorized binary #1 test came back POSITIVE within the clock. Pending human verification of the NA-union + exact-LM steps. | `rounds/round4_audit.md`. |
| 2026-06-22 | **Round 5 = the SODA-deciding crux: ONLINE LOCALIZATION, full regime** (owner: "先全力攻在线 localization" / "全区间 ondisc_k 是否 = Ψ"). Determine whether `ondisc_k = Θ(Ψ)` (online catches down to the offline floor via an L2→L∞ promotion) OR there is an online penalty `ondisc_k ≫ Ψ` (cf. A-T: online≫offline at k=2). Writing deferred. | Owner answer 2026-06-22. |
| 2026-06-22 | 🟢🟢 **Round 5 banked P12** (`ondisc_k=O(loglog n)` ∀k) ⇒ **fixed-k CLOSED `Θ(loglog n)`** (2 blind audits 85/88%). Now TWO complete tight results (P11 offline + P12 fixed-k online) + a multicolor online-vs-offline separation. **MATCH ruled out for SMALL k only** (correction); growing-k OPEN at the ranked-broadening lemma (RB). L2→L∞ no-go proven. | `rounds/round5_audit.md`. |
| 2026-06-22 | **GATE (b)/decision: owner chooses ATTACK RB FIRST, then write** (over the health-check's "write-first" recommendation — owner risk appetite, NO-RETREAT toward the strongest result). Round 6 = focused attack on RB (close growing-k to `Θ(max{Ψ, loglog n/k})`). 🔴 Orchestrator caveat (per `docs/HEALTH_CHECK_milestone_r5.md`): ONE focused RB round; if it does not close / hits a fundamental obstruction → pivot to writing (do not grind vs the 07-09 deadline). Two results remain "AI-verified, pending human verification". | Owner answer 2026-06-22. |
| 2026-06-22 | **Round 6 = RB OPEN (not closed).** Banked **P13 (BC** adaptive broadening-cost) + **B4** (symmetric `(R,s)` route provably gives `H/log k` not `H/k` — DEAD; a proof obstruction, not a lower bound). RB narrowed to the **Profile-Fibonacci lemma (PF)** via the asymmetric **LeftGap** rule (UNPROVEN, GPT 60%). Per the pre-committed plan → pivot-to-writing decision surfaced to the owner. | `rounds/round6_audit.md` (88%). |
| 2026-06-22 | **Owner: ONE more round on PF/LeftGap, then write** (over the write-now recommendation — owner risk appetite; the symmetric route is dead so this is the only live closure path). Round 7 = attack the Profile-Fibonacci lemma (the mixed-profile transport estimate for the LeftGap token process) ⇒ RB ⇒ growing-k closed. 🔴 If PF doesn't close → WRITE (RB→PF as open problem); no further RB/PF rounds. | Owner answer 2026-06-22. |
| 2026-06-22 | **Round 7 = displayed PF REFUTED (B5, phase conveyor), but NOT a lower bound ⇒ RB still OPEN.** The one authorized PF round did not close ⇒ **ATTACK LOOP CLOSED → PIVOT TO WRITING** (per the pre-committed plan). Open problem sharply mapped (RB ⟸ `PF_tr` ⟸ fold-or-Fibonacci; symmetric route B4 + naive route B5 both dead). Writing step 1 = `paper/FROZEN_RESULTS.md`. Binding venue risk = human verification of T1/T2/P13. | `rounds/round7_audit.md` (93%); `docs/HEALTH_CHECK_milestone_r5.md`. |
| 2026-06-22 | **Owner: VERIFY A/B/C via multiple adversarial agents** (treat AI-pass as pass; human check = post-submission referees); **SHELVE growing-k**; **dedup vs 2 siblings.** Outcome: V1/V2/V3 all VERIFIED (90/88/88%, incl. reproducing the A-T recursion); dedup CLEAN; READY TO WRITE. | `paper/VERIFICATION_CAMPAIGN.md`; `lit/DEDUP_sibling_projects.md`. |
| 2026-06-22 | **Writing step 2: skeleton + component-map + draft intro** (`paper/SKELETON.md`, structure-clone A-T). **FROZEN here by owner; resume writing after Thu 2026-06-25 (token budget). Pushed to GitHub.** Pending owner review: title/headline/venue. | Owner decision 2026-06-22. |
