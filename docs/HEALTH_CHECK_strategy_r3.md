# Strategy Health-Check — post-Round-3 inflection (independent subagent)

> Date 2026-06-22. Independent, blind, deliberately brutal strategy reviewer (read the ledger, all 3 round-audits,
> the gate-c health-check, the P7 scoop-scans). Verbatim conclusions + orchestrator disposition. This is the
> trajectory/venue assessment that triggers the owner touch.

## Bottom line (verbatim)
- **SODA-by-deadline (2026-07-09) probability: ~10–15%.** NOT a kill (uppers are open, the line produces real results),
  but a poor *venue* bet at this date.
- **The bind:** the most reachable real result (the **offline occupancy-threshold characterization** = P9 lower +
  saturation upper) is **ESA/RANDOM-grade**, because the upper is routine (Lovett–Meka partial coloring) and the model
  is average-case; the **only SODA-grade novelty (online localization)** is the **least reachable** (won't close in 2.5 wk).
  *Your reachable result isn't SODA-grade, and your SODA-grade result isn't reachable.*
- **5/5 "won't close as a clean SODA characterization" discriminators:** rounds produce only lowers+refutations (uppers
  static 0/5); target reshaped 3× (each subtracting from the headline); 5 distinct open lemmas; solver confidence dropping;
  different mechanism per band (online term vs occupancy floor) ⇒ a messy regime-patchwork, not one clean threshold.

## ① Reality check
After 3 strong rounds: a growing pile of LOWER bounds + refutations, ZERO matching UPPER anywhere — not bad luck, a
*signal* that the difficulty is the constructive/adaptive upper half. The headline dissolved 3× toward a static counting
floor and away from the online/A-T phenomenon. 2.5 weeks can't cover (identify+prove an upper)+(write)+(re-confirm SODA).

## ② Best remaining target — ranked
1. **Offline entropy-saturation upper (#1) — most reachable** (no adaptivity; Lovett–Meka on the shelf; P9 gives the
   matching lower). *The only one with a sharp lower AND standard upper machinery at the same threshold.* BUT the upper is
   routine/folklore-adjacent and the model is average-case ⇒ likely **ESA/RANDOM-grade, not SODA**; the novelty is real but
   thin and in the LOWER bound (wrong half for a top venue).
2. **Online localization (#2) — highest value, deepest, least controlled** (A-T: online≫offline at k=2; only an L2 tool P10;
   L2→L∞ is the cost). Not closeable in 2 weeks.
3. bounded-d CASCADE lower (#5) — needs the untouched ancestry-fiber lemma; only completes a picture whose upper is absent.
4–5. bounded-d FAST (#3) / CASCADE upper (#4) — need the same adaptive-stability objects that resisted every round. Lowest odds.

## ③ Escalate vs pivot vs downgrade — recommendation to the HUMAN GATE (agent presents, does not decide)
**A blend, not a single letter:** (a) **escalate ONE fresh-context attacker at the offline saturation upper (#1), full force,
hard 5-day clock, binary outcome** (respects "escalate, don't weaken"); (b) **re-aim the primary contribution at the offline
occupancy-threshold characterization** as the bankable result, online-localization as the flagged headline open problem;
(c) **pre-authorize the likely honest landing = a strong ESA/RANDOM paper** (the novel occupancy-entropy lower + matching
offline upper + the P6/P8/P10 toolkit + the d-independence-breaking framing + online-localization as the open problem) so
writing starts in parallel NOW rather than after a likely-failed SODA gamble.
**Do NOT run another open-ended "find more structure" solver round — that's the moving-target trap** (3× running: new lower +
reshape + closure-drop). The only worthwhile round is the narrow upper-only attack on #1 with a kill-clock.
> 🔑 "Escalate, don't weaken" governs the *research line* (keep it alive, escalate #1). It must NOT be used to stall the
> *venue* decision — that is a human gate the evidence says should fire now.

## ④ "Chasing a moving target?" — yes, a live risk; the 5-day #1 attack is the decisive test
If #1 closes in 5 days → a complete offline characterization → write the ESA/RANDOM paper with confidence. If it does NOT
close in 5 days → near-decisive evidence the upper half is hard even in the *easiest* (non-adaptive) regime → it's a
lower-bound theory to be written at the venue it can carry. Either way: a decision, not another reshape.

## Orchestrator disposition
- This is **gate-(a)/(c)** (venue/scope + pivot). Per the constitution the **owner decides**; the agent does NOT silently
  downgrade. I surface this candidly with the strategy assessment + a recommended path. The **research line stays alive**
  (NO-RETREAT): escalate the fresh attacker on #1 at full force regardless of the venue choice.
- I weigh the reviewer's view against the constitution: it does NOT claim proof-of-death (so no forced kill), and it
  correctly routes the venue call to the human gate. Its "ESA/RANDOM is the realistic venue" is a *recommendation for the
  owner*, not an agent downgrade. I present all three live options (keep pushing SODA / re-aim+ESA-RANDOM / blend) without
  pre-deciding.
- I do NOT over-index on one reviewer: I present the ~10–15% as its estimate, note the line is genuinely productive, and
  let the owner set risk appetite + deadline tradeoff. I will NOT launch another open-ended round; if escalating, it is the
  narrow #1 upper attack with a clock.
